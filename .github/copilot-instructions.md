# Kiro Specs 模式强制指令（Copilot 必须遵循）
## 核心流程（需求→设计→任务→编码，每步需用户确认）
1. 需求阶段：收到功能需求后，先输出结构化需求文档（requirements.md），包含：
   - 用户故事（As a... I want... So that...）
   - EARS 验收标准（可验证、可达成、可相关、明确）
   - 边界条件与异常场景（如未登录、数据为空、网络错误）
   输出后必须等待用户输入「需求确认」才可进入下一步。

2. 设计阶段：需求确认后，输出技术设计文档（design.md），包含：
   - 技术选型（框架、库、数据库）
   - 数据模型（表结构、DTO 定义）
   - API 设计（端点、请求/响应格式、认证方式）
   - 核心流程图（如数据流、业务逻辑流）
   输出后必须等待用户输入「设计确认」才可进入下一步。

3. 任务阶段：设计确认后，输出可执行任务清单（tasks.md），包含：
   - 任务拆解（按模块/文件拆分，每个任务≤4 小时）
   - 依赖关系（标注任务执行顺序）
   - 验收检查点（如单元测试覆盖率≥80%）
   输出后必须等待用户输入「任务确认」才可进入编码阶段。

4. 编码阶段：任务确认后，按任务清单生成代码，要求：
   - 代码符合项目编码规范（如函数驼峰、组件 PascalCase、TypeScript 类型定义）
   - 同步生成单元测试（如 Jest/Mocha）
   - 代码中添加关键注释，关联需求/设计文档中的对应条目。

## 输出格式要求
- 所有文档使用 Markdown 格式，文件名固定为 requirements.md、design.md、tasks.md
- 代码块使用 ```语言 包裹，测试用例放在 __tests__ 目录下
- 每次输出前标注当前阶段（如「【需求阶段】」「【设计阶段】」）。